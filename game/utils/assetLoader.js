// Just return the image URL without any processing
// The sprite sheets should have proper transparency already
// or we accept the background as-is

export async function loadAndProcessImage(url) {
    return url;
}

export async function loadAndProcessItemImage(url) {
    return url;
}
